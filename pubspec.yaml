name: constant_keys_generator
description: A genarator for create constant keys file from json file.
version: 1.0.4
repository: https://github.com/TKSVN/constant_keys_generator
issue_tracker: https://github.com/TKSVN/constant_keys_generator/issues

environment:
  sdk: '>=2.12.0 <4.0.0'

# Add regular dependencies here.
dependencies:
  glob: ^2.0.1
  yaml: ^3.1.0
  build: ^2.1.0

dev_dependencies:
  lints: ^1.0.0
  test: ^1.17.12

